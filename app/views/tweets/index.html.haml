= render "module/header"
.contents
  - @tweets.each do |tweet|
    .contents__box
      .contents__box__center
        - if @image.image.attached?
          .user_name_image_box
            .user_image
              = image_tag @image.user.image, size:"35x35", class:"image_css"
            .user_name
              %span
                = tweet.user.name
        %p.date
          = simple_time(tweet.created_at)
        .image__box
          - if tweet.image.attached?
            = image_tag tweet.image, size: "200x150"
        %h4.nameplate
          = tweet.name
        .text__area
          = simple_format(tweet.text)
        .text__end
          = link_to '続きを読む>', "/tweets/#{tweet.id}"
    .contents__space
  .paginate_box
    = paginate(@tweets)
